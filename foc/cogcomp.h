#ifndef COGCOMP_H
#define COGCOMP_H

#include "util/typedef.h"

#define COGCOMP_MAP_SIZE 360
typedef struct {
        f32 map[COGCOMP_MAP_SIZE];
} cogcomp_t;

static int write_map(f32) {}

#endif // !COGCOMP_H
